# Gong LoRa System

Автономная система гонга для курсов (изначально — под 12-дневный курс Випассаны):
один **сервер** с расписанием и веб-интерфейсом, и произвольное число
**клиентов**, которые слышат сервер по LoRa (433 МГц) и синхронно проигрывают
трек через MAX98357A. Ни сервер, ни клиенты не подключаются к интернету и не
зависят от роутера — сервер сам поднимает точку доступа Wi-Fi для веб-интерфейса,
а сервер↔клиент общаются напрямую по радио.

> Этот README описывает код и схему **как они есть в этом репозитории прямо
> сейчас**. Более ранняя версия README (до коммита `415825b`, «strip
> LoRa/client to keep server as WiFi-only») описывала WiFi STA + NTP +
> `MSG_SCHEDULE` как «просто лог» — ничего из этого в текущей прошивке нет.
> Если у вас уже распаяна плата по инструкциям из старого README — сверьте
> распиновку LoRa DIO0 ниже, это единственное расхождение, которое
> физически ломает связь без единой ошибки в логе.

---

## 1. Как это работает

```
┌────────────────────────┐         LoRa 433MHz          ┌────────────────────────┐
│        SERVER          │◄────────────────────────────►│        CLIENT ×N       │
│  ESP32 + Ra-02 + I2S   │   HEARTBEAT / GONG / STOP /  │  ESP32 + Ra-02 + I2S   │
│  + DS3231 + Wi-Fi AP   │   SCHEDULE (binary, signed)  │                        │
└──────────┬─────────────┘                              └──────────┬─────────────┘
           │ Wi-Fi AP (192.168.4.1)                                │
           ▼                                                       ▼
   Веб-интерфейс (браузер)                                    Динамик (MAX98357A)
```

- **Сервер** держит расписание (`/gong.conf` + шаблоны дней `/dayNN.conf`),
  раздаёт веб-интерфейс со своей же точки доступа, и в момент срабатывания
  записи шлёт по LoRa подписанный кадр `GONG` — все клиенты в радиусе слышат
  его и запускают тот же трек практически одновременно с сервером.
- **Клиент** — минимальное устройство без экрана и кнопок: слушает эфир,
  проверяет подпись, проигрывает MP3 с локальной SPIFFS/LittleFS. Отвечает на
  heartbeat сервера, чтобы попасть в список «живых клиентов» в веб-интерфейсе
  — период 30 с (`HEARTBEAT_INTERVAL_MS`). На SF10/BW125 (§2.2) сам цикл
  heartbeat (~0.8 с в эфире) + окно ACK из 8 слотов занимает ~7 с. Перед
  запланированным гонгом сервер heartbeat не начинает (§6).
- **Если сервер замолкает** (ни одного валидного кадра — heartbeat, GONG,
  STOP, SCHEDULE — перезагрузка, авария) дольше 10 минут, клиент
  переходит на **автономное расписание**, которое сервер заранее разослал по
  радио (см. §7) — курс не срывается из-за одного сбойного устройства.

---

## 2. Аппаратная часть

### 2.1 Общая для сервера и клиента распиновка

| Функция      | GPIO | Комментарий |
|---|---|---|
| LoRa NSS     | 5    | Strapping-пин — обязателен pull-up 10 кОм → 3.3V (см. §2.4) |
| LoRa RST     | 14   | MTMS/JTAG — pull-up 10 кОм → 3.3V + 100 нФ → GND рекомендован при длинных проводах |
| **LoRa DIO0**| **4**| **Не GPIO2!** GPIO2 — strapping-пин со встроенным синим светодиодом на многих DevKit-платах, мешает загрузке. Если прервано на GPIO2 — SPI-инициализация пройдёт успешно, пакеты будут физически приходить, но прерывание никогда не сработает: **полная тишина без единой ошибки в логе.** |
| LoRa SCK/MISO/MOSI | 18/19/23 | штатный VSPI |
| I2S BCLK     | 26   | MAX98357A |
| I2S LRC      | 25   | MAX98357A |
| I2S DOUT     | 33   | MAX98357A. На части плат GPIO32/33 разведены под внешний кварц 32.768 кГц — если на вашей плате он распаян, DOUT на 33 не заработает; проверьте конкретную ревизию. |
| I2C SDA/SCL  | 21/22| DS3231 (**только сервер**) |
| Status LED (клиент) | 13 | см. §2.5; `-1` отключает |

### 2.2 LoRa-модуль: Ra-01 или Ra-02 (SX1278)

![Ra-02 (SX1278)](images/ra-02.png)

Электрически идентичны — Ra-02 просто добавляет SMA-разъём под внешнюю
антенну. Модуль **не** выводит DIO1 (только DIO0 на стандартном 2×4
разъёме) — см. `footprint/lora-01.txt`. Отсюда одно прямое следствие: любая
защита от коллизий эфира, основанная на CAD (Channel Activity Detection),
здесь физически невозможна без замены модуля — прошивка вместо этого решает
проблему коллизий детерминированными тайм-слотами ACK (см. §6).

**⚠️ Не подавать питание на модуль, пока не подключена антенна.** Работа
выходного каскада SX1278 на разомкнутый выход необратимо выводит его из
строя.

Радиопараметры (заданы в `common/lora_shared.h`, ОБЯЗАНЫ совпадать на
сервере и всех клиентах — расхождение не даёт ошибки, просто устройства
перестают слышать друг друга):

| Параметр | Значение |
|---|---|
| Частота | 433.0 МГц |
| Spreading Factor | SF10 |
| Bandwidth | 125 кГц |
| Coding Rate | 4/5 |
| TX Power | 17 дБм (50 мВт ERP) |

Как читать RSSI/SNR во вкладке LoRa и реальные замеры на стенде — §12.

> 17 дБм превышает типичный лимит 10 мВт ERP для ISM 433 МГц в EU/РФ
> примерно в 5 раз. Для стационарной системы в одном здании это осознанный
> компромисс ради дальности и устойчивости связи — но это компромисс,
> который стоит зафиксировать сознательно, а не унаследовать по умолчанию.
>
> SF10/BW125 (с сентября 2026, см. §11) — чувствительность ~−132 дБм, на
> ~16 дБ хуже прежнего SF12/BW31.25 (−148 дБм), зато airtime кадра ~0.6–0.8 с
> вместо ~10 с, и полоса 125 кГц терпит уход кварца Ra-01/02 без TCXO с
> многократным запасом (при 31.25 кГц уход ±4–9 кГц был сравним с допуском
> модема ~7.8 кГц). Duty cycle: сервер ~3% (heartbeat 0.8 с раз в 30 с),
> каждый клиент ~2% — выше типичного лимита 1% для части суб-полос ISM
> 433 МГц; если это важно, увеличьте `HEARTBEAT_INTERVAL_MS`. История
> конфигов — `docs/lora-signal-research.md`. TX/ACK-таймауты
> и ширина ACK-слота считаются из фактического SF/BW через
> `radio.getTimeOnAir()` в рантайме, а не захардкожены — исторически именно
> хардкод под один SF был источником багов при следующей смене конфига.
> Рекомендация из `docs/lora-signal-research.md` — RF-bypass конденсатор
> (110–480 пФ) на питании Ra-02, максимально близко к модулю — **не
> подтверждена на этой ревизии железа**, стоит проверить сейчас.

### 2.3 MAX98357A (I2S-усилитель, 3 Вт @ 4 Ω)

![MAX98357A](images/max.png)

![Пиноут MAX98357A](images/max98357a_pinout.png)

- **SD → 1 МОм на VDD** (не на землю и не напрямую на 3.3V!). SD одновременно
  и «shutdown», и селектор канала по уровню напряжения; перемычка на 3.3V
  даёт «только левый канал». Резистор 1 МОм (с внутренней подтяжкой чипа
  100 кОм к земле) даёт ~0.3–0.45 В на SD → режим (L+R)/2.
- **GAIN**: в этой схеме оставлен висящим (NC) → 9 дБ. Альтернативы: GND напрямую
  (15 дБ), 100 кОм→GND (12 дБ), 100 кОм→VDD (6 дБ), VDD напрямую (3 дБ).
  Выбор нужно зафиксировать письменно вне зависимости от значения — «не
  подключено» здесь такое же осознанное решение, как и любое другое.
- **Выход мостовой (BTL)** — ни один вывод динамика нельзя сажать на землю,
  «минусы» двух усилителей объединять нельзя.
- **Отдельное сильноточное питание.** При максимальной громкости пиковый
  ток может доходить до 1.2–1.5 А — дорожки/защитный диод/USB-разъём
  типовой dev-платы на это не рассчитаны; просадка в момент удара гонга даёт
  brownout-перезагрузку ESP32 ровно тогда, когда должен звучать звук. См.
  `footprint/schematic_netlist.md` — там уже разведено отдельное 5В-питание с
  ферритовой развязкой (FB1) между цифровой и аудио-секцией, плюс bulk-ёмкости
  у обеих секций.
- Провода к динамику — витая пара, не длиннее 1-2 м, ферритовая бусина на
  выходе (Class-D шумит на частоте коммутации ~300 кГц и наводится на приёмник
  433 МГц в том же корпусе).

### 2.4 Discrete-доработки (сервер, см. `footprint/schematic_netlist.md`)

| Что | Зачем |
|---|---|
| Pull-up 10 кОм NSS → 3.3V | SX1278 не видит SPI-мусор, пока ESP32 грузится |
| Pull-up 10 кОм RST → 3.3V + 100 нФ → GND | гасит звон при длинных проводах к модулю |
| SD MAX98357A → 1 МОм → VDD | режим (L+R)/2 вместо «только левый канал» |
| GAIN — задокументированный выбор | см. §2.3 |

### 2.5 DS3231 (RTC) — только сервер, **обязателен**, не опционален

![DS3231](images/ds3231.png)

Сервер работает исключительно как автономная точка доступа — NTP в прошивке
нет вообще. Без DS3231 время сбрасывается при каждом отключении питания,
расписание блокируется до ручной установки времени через веб-интерфейс, а
автопереход по дням курса (`dayNN.conf`) завязан именно на календарную дату.

⚠️ **Батарейка.** Дешёвые модули ZS-042 имеют встроенную цепь подзарядки под
LIR2032. Обычный CR2032 в такой цепи постоянно «подзаряжается» — вздутие,
течь, в пределе разрушение модуля. Либо выпаять зарядный резистор на модуле
(маркирован `R5`/`201`), либо ставить перезаряжаемый LIR2032.

### 2.6 Статусный светодиод клиента

Клиент — устройство без экрана, сети и кнопок; понять «жив ли он, слышит ли
сервер» без ноутбука по USB можно только по этому светодиоду
(`client/src/config.h: STATUS_LED`, по умолчанию GPIO13):

| Индикация | Значение |
|---|---|
| 4 коротких мигания при старте | загрузка прошла |
| Быстрое непрерывное мигание (~5 Гц) | радио не инициализировалось — проверьте пайку/DIO0 |
| Одно короткое мигание раз в 3 с | норма, heartbeat получен недавно |
| Двойное мигание раз в 3 с | heartbeat не приходил > 10 минут — клиент работает по автономному расписанию (§7) |
| Горит непрерывно | сейчас играет трек |

### 2.7 Питание и заземление (сервер, HLK-10M05 5В/2-3А)

Схема в `footprint/schematic_netlist.md` реализует:
- отдельный БП 5В с достаточным запасом по току;
- заземление звездой в одной точке;
- 1000 мкФ у входа платы, 470–1000 мкФ у VDD усилителя (короткими дорожками),
  100 мкФ+100 нФ у 3.3V ESP32, 100–220 мкФ+100 нФ у VCC LoRa-модуля;
- ферритовую развязку (FB1) аудио-секции от цифровой 5V-шины.

Проверка после сборки: осциллограф на 3.3V во время удара гонга при
максимальной громкости — просадка ниже 3.0V означает brownout.

---

## 3. Прошивка и загрузка

Оба проекта — независимые PlatformIO-окружения (`server/`, `client/`).

```bash
# Ключ HMAC — ОБЯЗАТЕЛЬНО одинаковый на сервере и ВСЕХ клиентах.
# Задаётся в common/secrets.h (файл в .gitignore — репозиторий публичный):
#   cp common/secrets.h.example common/secrets.h   # и вписать ключ
# Минимум 8 символов. Без secrets.h в config.h остаётся плейсхолдер
# (прошивка соберётся, но громко предупредит в логе и в /api/status).

cd server
pio run -t upload       # прошивка
pio run -t uploadfs      # данные (index.html, .conf, mp3) — LittleFS!

cd ../client
# Один из двух способов различать клиентов:
#  а) ничего не делать — ID выводится из MAC-адреса автоматически (по
#     умолчанию, см. H-6 в 02_FIXES.md — раньше дефолт легко забывали менять
#     на втором устройстве, и оба клиента сливались в одну запись сервера);
#  б) задать говорящее имя: раскомментировать CLIENT_ID_OVERRIDE в config.h.
pio run -t upload
pio run -t uploadfs
```

Альтернатива `secrets.h` — переменная окружения: `export GONG_KEY=...` и
раскомментировать строку `-DLORA_HMAC_KEY=...` в `build_flags`
соответствующего `platformio.ini`. Если ключ не задан никак — прошивка
соберётся с плейсхолдером-предупреждением (`change_me_before_deploy_32chars!`),
что валидно для стенда/разработки, но не для реального курса. Короткий
словарный ключ подбирается перебором по одному перехваченному кадру — для
боевой эксплуатации берите длинный случайный.

**Файловая система — LittleFS, не SPIFFS.** SPIFFS в arduino-esp32 — legacy:
деградирует по мере заполнения, нет директорий, лимит имени 32 символа, и
хуже переживает потерю питания посреди записи — а питание здесь пропадает
регулярно (это и есть причина существования watchdog/self-heal в прошивке).
`pio run -t uploadfs` форматирует раздел под LittleFS автоматически.

### Обновление веб-интерфейса

`server/data/index.html` отдаётся в сжатом виде, если рядом лежит
`index.html.gz` (41 КБ → ~9 КБ — заметно быстрее по Wi-Fi AP). После правки
`index.html` пересоздайте архив перед `uploadfs`:

```bash
cd server/data && gzip -9 -k -f index.html
```

---

## 4. Веб-интерфейс сервера

После прошивки поднимается точка доступа `GongServer` (пароль —
`AP_PASSWORD` в `server/src/config.h`), интерфейс — `http://192.168.4.1` или
`http://gong.local` (mDNS).

- **Schedule** — расписание активного дня курса; можно листать дни курса
  вперёд/назад (только просмотр, `GET /api/day?n=N`, ничего не переключает)
  и явной кнопкой «Activate this day» переключить активный шаблон.
- **Clock** — источник времени: RTC (DS3231) или ручной ввод. Расписание не
  работает, пока время не установлено (часы показывают `--:--:--`).
- **Security** — пароль администратора (PBKDF2-SHA256, см. §8), по умолчанию
  выключен — интерфейс открыт всем на точке доступа.
- **LoRa** — список клиентов, ответивших за последние ~4 цикла heartbeat (RSSI, SNR,
  время последнего ответа), ручное проигрывание (локально / по радио / оба).

Один запрос `/api/state` раз в 5 секунд обновляет все карточки разом — это
не пять параллельных запросов, как было раньше (см. §9, M-10): однопоточный
`WebServer` ESP32 обслуживает одно соединение за раз, и пять запросов подряд
на каждый тик означали, что реальное действие оператора (правка расписания)
вставало в очередь позади поллинга и срабатывало с задержкой в минуты.

---

## 5. Многодневный курс (`dayNN.conf`)

Ключевая функция системы — не задокументированная вообще в исходном README.
12 «шаблонов дня» (`/day00.conf` … `/day11.conf`, `DAY_COUNT` в `config.h`):
`day00` = день заезда, `day01`–`day10` — одинаковый «боевой» распорядок,
`day11` = день отъезда. «День» — номер дня курса, не день недели и не
календарная дата.

- `/gong.conf` — активное расписание, реально проверяемое рантаймом.
- `/activeday.conf` — `{"day": N, "date": "YYYY-MM-DD"}`, какой шаблон активен
  и на какую календарную дату он был активирован.
- Автопереход на следующий день срабатывает по смене календарной даты (не по
  тику «00:00»), и переходит сразу на нужное число дней вперёд, если
  устройство было выключено несколько суток подряд (не залипает на +1).
- Каждая запись расписания несёт трек, число повторов **и громкость** — тихий
  подъём в 04:00 и громкий общий сбор в 08:00 можно задать по-разному.
- **После `day11` автоперехода нет** — это осознанное поведение, не баг:
  устройство остаётся на дне 11 (лог: «course ended, staying on day 11»), в
  вебе видна плашка «Курс завершён». Для нового заезда активируйте `day00`
  вручную — кнопкой «Activate this day».

---

## 6. Протокол LoRa

Кадр: `[1 байт тип][8 байт HMAC-SHA256/32 тег][payload]`. Тег — на
фиксированном смещении, считается напрямую по байтам, ушедшим в эфир — без
пересборки JSON на приёмнике (более ранняя схема подписывала JSON строковой
хирургией и проверяла через parse→remove→re-serialize, что работало только
пока порядок полей совпадал побайтово).

| Тип | Кто шлёт | Подписан | Payload |
|---|---|---|---|
| `GONG` (0x01) | сервер | да | JSON: track/vol/loop/ts/n |
| `HEARTBEAT` (0x02) | сервер | да | JSON: time/clients/ts/n/seq |
| `SCHEDULE` (0x03) | сервер | да | бинарный: ts + n + до 16 записей активного дня |
| `ACK` (0x04) | клиент | да | JSON: id/rssi/[hb]/sh (отпечаток сохранённого расписания, §7) |
| `STOP` (0x05) | сервер | да | JSON: ts/n |

- **Anti-replay**: `ts` — монотонная метка (реальное время либо
  NVS-подстрахованный аптайм на сервере), клиент отбрасывает всё
  `ts < последний_виденный`, значение сохраняется в NVS — переживает
  перезагрузку клиента. Метка отслеживается **отдельно для каждого типа
  кадра**: GONG/STOP обгоняют уже стоящий в очереди HEARTBEAT/SCHEDULE, и с
  одной общей меткой такой heartbeat отбрасывался бы как «повтор». `ts` даёт разрешение в 1 секунду — этого мало,
  чтобы отличить, например, Stop сразу после Play; `n` — простой счётчик
  отправок за это включение сервера (не переживает его перезагрузку, это и
  не нужно — `ts` уже гарантированно прыгает вперёд после ребута) — разбивает
  кадры с одинаковым `ts` по порядку: кадр принимается, если `ts` больше
  последнего виденного, либо `ts` тот же, а `n` больше.
- **Коллизии ACK**: клиент отвечает в детерминированном слоте, вычисленном
  из хеша своего `CLIENT_ID` и `seq` heartbeat (`ACK_SLOT_COUNT=8` слотов;
  слот меняется каждый цикл, поэтому совпадение слотов двух клиентов стоит
  одного цикла, а не делает пару невидимой навсегда). Ожидание слота не
  блокирует приём — GONG/STOP, пришедшие в это время, обрабатываются сразу.
  Это вместо случайного
  джиттера 0–70 мс, который на SF9 (~288 мс airtime ACK) гарантированно
  накладывался уже при двух клиентах. Ширина слота считается через
  `radio.getTimeOnAir()` от фактического SF/BW при старте радио (не
  хардкод — см. §2.2), поэтому не расходится с реальным
  airtime при следующей смене конфига. ACK на `GONG` не отправляется вовсе —
  сервер его не использует, а держать эфир занятым ровно тогда, когда следом
  может прийти `STOP`, было лишним риском.
- **Приоритеты эфира на сервере.** GONG/STOP — отдельная приоритетная
  очередь, уходят сразу, окно ACK не ждут. HEARTBEAT ставится в очередь
  только когда эфир свободен и окно ACK закрыто (иначе его поле `time`
  устаревало бы на всё окно). SCHEDULE ждёт закрытия окна. Приём ACK
  обрабатывается раньше передачи. Heartbeat не начинается, если до
  ближайшего гонга по расписанию меньше, чем длится цикл heartbeat+ACK:
  клиент, передающий свой ACK, не слышит эфир и пропустил бы GONG.
- **Локальное проигрывание — не блокирует.** И на сервере, и по всей цепочке
  TX — асинхронно: планировщик шлёт кадр в очередь и продолжает работать;
  локальный трек стартует, когда фактически завершилась передача (или истёк
  таймаут), а не когда кто-то полсекунды ждал в цикле.

---

## 7. Автономный режим клиента (резервное расписание)

Самый тяжёлый операционный риск исходной системы: сервер — единая точка
отказа, и его смерть ночью означала, что подъём в 04:00 не прозвонит нигде.

Решение: сервер рассылает расписание активного дня в бинарном виде (влезает
в один LoRa-кадр — то же расписание в JSON, ~3.4 КБ, никогда бы не влезло) —
при активации дня и раз в час. Кроме того, клиент в каждом ACK сообщает
короткий отпечаток (`sh`) сохранённого у себя расписания; если он не совпадает
с последним разосланным (клиент был выключен во время рассылки, например в
полночь при смене дня), сервер повторяет рассылку в течение минуты, а не ждёт
ежечасной. Клиент хранит его в NVS вместе с виртуальными
часами, которые синхронизирует по полю `time` из каждого heartbeat с
поправкой на время кадра в эфире (у клиента нет RTC — точность ровно такая,
какой была последняя heartbeat; после перезагрузки клиента часов нет до
первого heartbeat).

Если heartbeat не приходил дольше 10 минут (`HEARTBEAT_LOST_MS`), клиент
переходит на автономное расписание и звонит по нему сам, раз в минуту сверяя
виртуальные часы с сохранённым расписанием. Как только сервер снова слышен —
клиент снова становится ведомым. Если сервер вернулся и прислал тот же трек
в течение 5 минут после автономного срабатывания — клиент его пропускает
(чтобы не звонить дважды).

---

## 8. Безопасность

- **HMAC-ключ** (`LORA_HMAC_KEY`) — без него любой, у кого есть Ra-02 и этот
  репозиторий, может позвонить в гонг когда угодно. Плейсхолдер в
  `config.h` собирается (чтобы проект работал «из коробки» на стенде), но
  громко предупреждает в логе и в `/api/status` (`default_key: true`).
- **Пароль администратора** — PBKDF2-SHA256, 20000 итераций, случайная соль
  16 байт, константное по времени сравнение. Хранится в `/auth.conf` как
  `{"salt","hash","iter"}` — не как открытый текст.
- **CSRF**: у веб-интерфейса нет CORS (незачем — он same-origin), но все
  изменяющие запросы должны нести заголовок `X-Gong-Request: 1`, который
  сторонняя страница выставить не может без CORS-preflight, а preflight
  ничем не отвечен.

---

## 9. Известные ограничения

- Нет OTA — обновление каждого клиента требует физического USB-подключения.
- Автономное расписание клиента ограничено 16 записями активного дня и живёт
  только по виртуальным часам от последнего heartbeat — не замена RTC.
- Радиорегуляторика: TX Power 17 дБм превышает типичный лимит для ISM
  433 МГц в EU/РФ — см. §2.2.
- Тестов и CI нет.

---

## 10. Структура репозитория

```
server/         прошивка сервера (PlatformIO)
client/         прошивка клиента (PlatformIO)
common/         lora_shared.h — общие RF-константы, типы сообщений, формат кадра
docs/           lora-signal-research.md — история RF-конфигов, открытые аппаратные вопросы
footprint/      схема (EasyEDA JSON), netlist, черновая расстановка PCB
images/         скриншоты веб-интерфейса, пиноут MAX98357A
01_AUDIT_REPORT.md   аудит проекта — блокеры/риски/находки
02_FIXES.md          конкретные патчи по каждому пункту аудита
```

---

## 11. Аудит логики LoRa (сентябрь 2026)

Разбор кода сервера и клиента выявил, что при прежних радиопараметрах
**SF12/BW31.25** (кадр ~10 с в эфире, окно ACK 16 слотов ≈ 190 с) связка
«очередь TX + окно ACK» ломала основную функцию — гонг вовремя. Всё ниже
выведено из кода; исправления в ветке `fix-lora-queue-timing`, на железе
ещё не проверены.

### 11.1 Что было найдено

**🔴 Критично — очередь TX на сервере**

1. **Heartbeat почти всегда стоял первым в очереди.** `lora_sendHeartbeat()`
   ставил кадр, как только очередь пуста; во время окна ACK он ждал в голове
   очереди, а стоило ему уйти — через 2 с вставал следующий. Очередь
   практически никогда не была пустой.
2. **GONG/STOP застревали за ним.** Проверялся только первый элемент
   (`xQueuePeek`) — GONG за ждущим heartbeat ждал конца окна, до ~3 минут.
   Серверный динамик ждал того же (локальный трек стартует после окончания
   передачи). **Весь гонг — сервер и клиенты — звучал с опозданием от 0 до
   ~3.5 минут.**
3. **Сервер не читал ACK клиентов.** Ветка приёма выполнялась только при
   пустой очереди — список клиентов в вебе в установившемся режиме был
   пустым.
4. **Время в heartbeat устаревало.** Поле `time` заполнялось при постановке
   в очередь, а в эфир кадр уходил через ~200 с — часы клиентов для
   автономного режима отставали примерно на 3 минуты.
5. **Клиенты отбрасывали heartbeat как повтор.** GONG, пущенный вне
   очереди, нёс более новый `ts`; клиент принимал его, а потом отбрасывал
   ждущий heartbeat как «старый».

**🔴 Клиент блокировался, пока ждал свой слот ACK.** Простой `vTaskDelay`
до ~3 минут: GONG/STOP, пришедшие за это время, обрабатывались только после
пробуждения. Пока клиент сам передаёт ACK (~9 с), он глухой — GONG в этот
момент терялся.

**🟡 Средняя серьёзность**

- **Двойной гонг**: heartbeat приходил редко и иногда отбрасывался, порог
  «сервер пропал» 10 минут — клиент мог уйти в автономный режим при живом
  сервере и сыграть трек дважды.
- **Перевод даты назад продвигал курс**: `if (diff <= 0) diff = 1;` —
  исправление даты на день назад сдвигало курс на день вперёд.
- **Переполнение `millis()` через ~49.7 суток**: `millis() < ackWindowUntil`
  могло застрять в true почти на 49 дней — heartbeat перестали бы уходить.
- **Последний день**: день зажимался до 11, `day11.conf` существует —
  каждую ночь день 11 заново активировался и рассылался.
- **Часы клиента не переживают перезагрузку** — если свет моргнёт у всех и
  сервер не поднимется, автономный режим не включится (не исправлено, без
  RTC на клиенте не лечится).
- **Замена сервера / стирание NVS без установленного RTC**: `ts` начинается
  почти с нуля, клиенты хранят последний виденный в NVS и отбрасывают всё,
  пока на сервере не выставят время (не исправлено — операционная заметка).

**🟡 Радио**: BW 31.25 кГц на Ra-01/02 без TCXO — уход кварца ±10–20 ppm
(±4–9 кГц на 433 МГц) сравним с допуском LoRa ~¼ BW ≈ 7.8 кГц; Semtech
рекомендует TCXO для полос уже 62.5 кГц. Пары модулей могли не слышать
друг друга, выглядело бы как «плохие антенны».

### 11.2 Что исправлено

**Сервер** (`server/src/lorahandler.cpp`, `main.cpp`, `schedule.cpp`)
- GONG/STOP — отдельная приоритетная очередь, уходят сразу, окно ACK не ждут.
- Приём обрабатывается до передачи — ACK снова читаются.
- Heartbeat ставится в очередь только когда эфир свободен и окно ACK
  закрыто — поле `time` свежее.
- Heartbeat не начинается, если до ближайшего гонга по расписанию меньше,
  чем длится цикл heartbeat+ACK (иначе клиент, передающий ACK, не услышал
  бы GONG).
- Окно ACK отслеживается устойчиво к переполнению `millis()`.
- Дата назад больше не сдвигает курс; последний день не активируется
  заново каждую ночь.

**Клиент** (`client/src/lorahandler.cpp`)
- Ожидание слота ACK не блокирует приём — GONG/STOP играются сразу.
- Слот ACK меняется с каждым heartbeat (хеш id+seq) — совпадение слотов
  двух клиентов стоит одного цикла, а не делает их невидимыми навсегда.
- Anti-replay отдельно по типам кадров (старое значение из NVS переносится).
- Сервер считается живым при любом валидном кадре, не только heartbeat.
- Часы клиента поправлены на время кадра в эфире.
- Server GONG того же трека в течение 5 минут после автономного
  срабатывания пропускается — нет двойного гонга.
- Порог перезапуска радио при тишине — 10 минут вместо 5.

**Общее**: `ACK_SLOT_COUNT` 16 → 8 (по BOM 4 клиента); радиопараметры
SF12/BW31.25 → **SF10/BW125** (§2.2); минимальная длина HMAC-ключа — 8
символов; ключ вынесен в `common/secrets.h` (в `.gitignore`).

**Совместимость**: формат кадра не менялся, но **перепрошить нужно сервер и
все клиенты** — и из-за исправлений, и из-за смены SF/BW (устройства с
разными SF/BW друг друга не слышат).

### 11.3 Что проверить на стенде

1. GONG по расписанию звучит на сервере и клиентах примерно через 1 с после
   назначенного времени.
2. Все клиенты появляются во вкладке LoRa в течение минуты-двух.
3. В логе сервера строка `Airtime-derived timing`: окно ACK ≈ 6 с.
4. Дальность на SF10/BW125 ниже, чем была на SF12 (~16 дБ) — проверить
   самую дальнюю точку установки клиента.

### 11.4 Результаты стенда (сентябрь 2026)

- ✅ Сервер и клиент на SF10/BW125 с общим ключом: клиент регистрируется,
  ACK принимаются (замеры сигнала — §12.2).
- ✅ Веб-страница на iPhone: отображалась «иероглифами» — `index.html.gz`
  отдавался с некорректным заголовком `Content-Encoding` (сначала дважды,
  через явный заголовок и автоопределение `streamFile()`). Теперь заголовок
  ставится ровно один раз явно, плюс `Cache-Control: no-cache`, чтобы браузер
  не показывал закэшированный битый ответ. Исправление подтверждено.
- ✅ DS3231 на сервере: найден, время переживает отключение питания.
  Найдена и исправлена ловушка: при пустом поле Date сервер подставлял
  2024-01-01 (теперь дата обязательна / берётся с телефона), а последующая
  установка настоящей даты перескочила бы курс на последний день (теперь
  скачок больше длины курса считается исправлением часов).
- ✅ Смена дня курса в полночь: `day 00 -> 01` по смене календарной даты,
  новое расписание сразу разослано клиентам (после окна ACK heartbeat).
- ✅ Клиент регистрируется сразу после включения, ACK на каждый heartbeat
  (~30 с).
- ✅ Ручной гонг по радио доходит до клиента (RSSI −38 на столе).
- 🔧 Клиент, выключенный во время полуночной рассылки, после включения
  держал резервное расписание дня 00 при активном дне 01 (до ежечасной
  рассылки). Добавлен отпечаток расписания в ACK — сервер досылает
  расписание в течение минуты (§7). Требует перепрошивки сервера и клиентов.
- ✅ Досылка по отпечатку проверена: клиент выключен → расписание изменено →
  клиент включён → сервер: `has an outdated schedule — will re-broadcast`,
  клиент сохранил актуальный `day=01 entries=11`. Лишнее повторное «outdated»
  на ACK, пришедшем пока рассылка ещё стояла в очереди, убрано.
- ✅ Гонг по расписанию: `Trigger 00:30` → GONG в эфир (кадр 61 байт, ~0.8 с)
  → локальное проигрывание на сервере по окончании передачи; клиент принял
  GONG (RSSI −46) и заиграл одновременно с сервером. Heartbeat и ACK перед
  гонгом завершились до срабатывания — эфир к гонгу был свободен.
- ✅ Стоп по радио сразу после ручного гонга: STOP (кадр 33 байта) ушёл
  следом за GONG, клиент принял оба по порядку (`GONG → ...`, затем
  `STOP → stopping audio`) — сценарий «Stop сразу после Play», который
  раньше мог отбрасываться защитой от повторов, работает.
- ⏳ Автономный режим, самая дальняя точка.

---

## 12. Уровень сигнала: RSSI и SNR

Вкладка **LoRa** веб-интерфейса показывает для каждого клиента RSSI и SNR
последнего принятого ACK — то есть уровень сигнала **клиента на стороне
сервера**. Это главный инструмент проверки антенн и места установки.

- **RSSI** (дБм) — мощность принятого сигнала. Чем ближе к нулю, тем сильнее.
- **SNR** (дБ) — насколько сигнал выше шума. LoRa умеет принимать и при
  **отрицательном** SNR (сигнал ниже шума); при SNR < 0 значение RSSI уже в
  основном отражает шум эфира, поэтому на краю дальности смотрите на SNR.

### 12.1 Как оценивать (SF10 / BW125)

Предел модема на SF10/BW125: чувствительность ≈ −132 дБм, SNR ≈ −15 дБ.

| RSSI, дБм | SNR, дБ | Оценка | Что делать |
|---|---|---|---|
| выше −90 | выше +5 | отлично | ничего |
| −90 … −110 | 0 … +5 | хорошо | ничего |
| −110 … −125 | −10 … 0 | на грани | проверить антенну, поднять/переставить клиент |
| ниже −125 | ниже −10 | предел | связь будет рваться; переставить клиент или вернуться к SF12 (с оглядкой на §2.2) |

**Ориентир для проверки антенн (измерен, §12.2):** с рабочими антеннами на
433 МГц сервер и клиент рядом на столе дают RSSI около **−40 дБм**, в точке
«кухня» (~8 м, два угла) — около **−70 дБм**. Если на столе заметно хуже
(−50 и ниже) — ищите потери: антенна не на 433 МГц (Wi-Fi 2.4 ГГц, 868 МГц),
отошёл разъём IPEX, или выходной каскад модуля повреждён (включение без
антенны, см. §2.2). Чисто расчётная цифра для идеальных антенн (−5 … −15 дБм
на 1 м) на практике недостижима — КПД дешёвых антенн, их взаимная
ориентация и близость платы съедают 20–30 дБ, поэтому сравнивайте с
замером, а не с расчётом.

**Антенна:** четвертьволновый штырь на 433 МГц — провод ≈ **17 см** от точки
пайки, либо покупная антенна именно на 433 МГц. Антенны на обоих концах
одинаково важны: плохая антенна на любой стороне съедает 10–40 дБ.

### 12.2 Замеры на стенде (сентябрь 2026, SF10/BW125, 17 дБм)

| Точка | Антенны | RSSI, дБм | SNR, дБ | Итог |
|---|---|---|---|---|
| Рядом, на столе | исходные | −53 | 13.5 | связь есть, но на ~13 дБ хуже, чем с рабочими антеннами (см. ниже) |
| Кухня: ~8 м, два угла | исходные | — | — | связь пропала |
| Кухня: ~8 м, два угла | одна антенна заменена | −81 | 10.3 | связь уверенная, запас ~50 дБ |
| Кухня: ~8 м, два угла | заменена и вторая антенна | **−71** | **14.0** | +10 дБ к предыдущему, запас ~60 дБ — **эталон** |
| Рядом, на столе | обе новые | **−40 … −45** | **10 … 12.5** | **эталон для стола** — +10…13 дБ к исходным антеннам |

Выводы:
- Пропадание связи на 8 метрах было вызвано **антеннами**, а не SF10 и не
  прошивкой.
- Текущие антенны — эталон: каждый новый клиент стоит проверять на столе
  (около −40 дБм) и в точке «кухня» (около −70 дБм) — такие цифры означают,
  что антенна и модуль в порядке.
- Остаётся проверить самую дальнюю точку установки клиента.

---

## Скриншоты

![Web UI 1](images/web1.png)
![Web UI 2](images/web2.png)
![Web UI 3](images/web3.png)
